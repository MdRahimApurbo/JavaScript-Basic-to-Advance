# JavaScript Date Object

first create a new object then call method we get date

```javascript
var obj = new Date();

document.write(

    // obj.getDate();
    // obj.getDay();
    // obj.getFullYear();
    // obj.getMonth();
    obj.getMinutes();


);
```
